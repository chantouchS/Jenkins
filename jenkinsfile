pipeline {
    agent any 
    stages {
        stage('Deploy') {
            steps {
                sh 'pwd'
                sh 'export'
                sh 'az-webapp-create-py.sh'
            }
        }
    }
}
