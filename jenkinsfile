pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                sh 'echo "Build : Python - nothing to do"'
            }
        }
        stage('Test') {
            steps {
                sh 'echo "Test : not implemented"'
            }
        }
        stage('Deploy') {
            steps {
                sh 'pwd'
                sh 'export'
                sh 'az-webapp-create-py.sh'
            }
        }
    }
}
