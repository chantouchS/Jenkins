pipeline {
    agent any 
    stage('Build') {
            steps {
                sh 'echo "Build : Python - nothing to do"'
            }
        }
        stage('Test') {
            steps {
                sh 'echo "Test : not implemented"'
            }
        }
    }
}
